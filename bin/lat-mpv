#!/usr/bin/env ruby
# frozen_string_literal: true

require 'rubygems'
require 'bundler/setup'
require 'lat'

path = File.expand_path('./../spec/blacklist.txt', __dir__)
Lat::Blacklist.default = Lat::FileBlacklist.new(path)
Lat::MpvScript.new(Lat::Mpv.client).runloop
